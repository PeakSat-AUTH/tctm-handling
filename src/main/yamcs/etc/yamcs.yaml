services:
- class: org.yamcs.http.HttpServer
  args:
    port: 8090
    cors: {allowOrigin: '*', allowCredentials: false}
dataDir: yamcs-data
instances: [OBC, ADCS]
buckets:
- {name: files, path: ../../src/main/resources/received}
secretKey: changeme
yamcs-web: {staticRoot: /home/nik/yamcs-instance/src/main/frontend/webapp/dist/opsmci}
